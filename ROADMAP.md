# しりすこスクワッド ロードマップ / 引き継ぎメモ

最終更新: 2026-09-04 (自宅Mac)。**個人利用のツールとして作り替え済み・公開中**。

> **▶ 引き継ぎ (2026-09-04 職場PC → 次)**: 作業ツリーは綺麗・全部 push 済み・CI 緑。始めるときは `git pull` から。
> 今日入れたもの: 使い勝手監査22件のうち18件 (筆頭: 計算報告の嘘・行計算の無反応・盤面上書きの undo・
> 型の被り診断) / 全モーダル共通の焦点管理 / **«キューブが効かない» の根治** (エンジンは無罪 —
> cube 無指定 = 既定キューブで計算する仕様を画面が黙っていた。実効キューブを ◦ 付きで表示、
> 橋のテスト3件で見張り) / ⑦ 韓国語コメントの日本語化 **10ファイル・814行** (途中)。
>
> **⑦ の残り (次にやること・約190行)** — サブエージェントの枠切れで止まった小物26ファイル:
> `site/src/` の burst-order.ts(32) model.ts(28) worker-client.ts(17) nikke-search.ts(15) csv-import.ts(13)
> ui.ts(12・大半は日本語文中の内部キー引用なので «意図的» か確認) display-name.ts(3) my-roster.ts(1) stat-names.ts(1) vite-env.d.ts(5)
> とテスト model.test.ts(19) share-code.test.ts(19) runtime-assets.test.ts(14) timeline.test.ts(8) blablalink.test.ts(7)
> csv-import.test.ts(6) raid-board.test.ts(6) worker-client.test.ts(5) nikke-search.test.ts(3) burst-order.test.ts(1)、
> `site/scripts/` の export-settings.py(24) sync-runtime.mjs(11) check-pages.mjs(2) check-runtime.mjs(2) e2e-board.mjs(2)、
> `site/public/calculator.worker.js(4)`。加えて翻訳済みファイルに**行末コメント** (コード行の後ろの //) が
> 未訳のまま残る: timeline.ts 11行・blablalink.ts 2行・ui.test.ts 5行。
> **やり方** (確立済み): 対象行 = 行頭が // /* * # <!-- でハングルを含む行。Python で行番号→新テキストの
> 辞書を作り assert 付きで置換、最後に一度だけ書く。**内部キー・データ値 (없음/작열…/キャラ名/CSVヘッダ/
> キューブ名) は訳さない**。既に日本語の行は触らない。終わったら
> `python site/scripts/check-comment-only.py` (**コメントを剥いだ骨格が編集前後で一致 = コード無傷**の
> 機械証明) → tsc・vitest・build・配色・`python site/scripts/test-bridge.py`。
> `site/public/runtime/**` はエンジンの同期コピーなので**触らない** (bridge.py は pybridge/ の原本を訳す)。
> **翻訳の申し送り**: エージェントが幾つかの UI 文言引用を現行の日本語ラベルに合わせて訳した
> (톡톡이→タップ撃ち、족자/속저→回避区間/属性制限、정밀 분석→精密分析 (既訳なし・要統一))。
> blablalink.ts:90 のコメントは実装 (equipLevelsOf) より古い可能性 — 翻訳のみで内容は直していない。
>
> **⑦ の後の候補**: 取り込み直しで候補の育成値が古いことの警告 / 使い勝手監査の軽微4件
> (型の名前・タブの矢印キー・モーダルの Tab 順・実機スマホ) / あなたの実測値との突き合わせ (最重要)。

> **⚠ 締切は無い** (2026-09-02 ユーザー明言)。第44回ユニオンレイド (9/4〜) に間に合わせる開発では**ない**。
> このサイトは本家しりすこPADとは**分けて開発**しており、PAD の都合にも縛られない。
> **急がず、確実に積む**方針。「レイドまでに」を判断の理由にしないこと — 過去に一度その前提で
> 急かしてしまい、ユーザーから明確に否定されている。

**2026-09-02 (自宅Mac) 追加: キューブ込みの登録と、属性3択の理論値最大** (ユーザー要望):
- 案 (`element-plans`) は**個別設定のスナップショット (キューブ等) 込み**で保存される
  (`characters` — 入っているニケはその値、無いニケはロスター任せ)。「3案を比較」で出した
  ダメージは案に**登録** (`registered` — 値と戦闘時間) され、再読込しても残る
- 盤面の枠も案から入れるとスナップショットを運び (`BoardSlot.characters`)、計算に使う
- **「属性で組む」**: 属性を3つ選ぶ (同属性2回以上可 — 同じボスに複数回凸できる) と、
  各属性の候補から**同じニケを使わない**割り当てで理論値合計が最大のものを3枠に入れる
  (`bestForElements` — 3^3=27 通りの総当たり。埋まる枠数を最優先、同数なら合計)
- 流れ: 枠やデッキで編成 (キューブ込み) を組む → 候補に加える/保存 → 比べる (=計算して登録) →
  「属性で組む」で 水冷・水冷・灼熱 のような組を選んで被りなしの3凸と理論値を得る
- **入れ替えのループは盤面で完結する**: 「候補をぜんぶ計算して比べる」も結果を**登録**し (再読込後も残る)、
  弱かった候補は行の ✕ でその場で消せる (3件上限に当たっても属性別編成タブへ移らなくてよい)。
  実機E2E `scripts/e2e-loop.mjs` の流れ (組む→候補に加える→計算→入れ替え→候補に加える→まとめて比較) を通して確認

**次にやること = 「🧭 構造と使い勝手の評価」の改善バックログの上から** (2026-09-03 に一本化):
①**取込データの端末間の持ち運び** (バックログ#1 = 実使用の不便①と同一。「この端末のデータを書き出す」、
  貼る側 `NKP1-` は動作済み) → ②S労力のUX3点セット (#2 最適化ボタンの命名分け / #3 ステータス行 /
  #4 呼称の統一)。どの PC からでも、この順に上から取ればよい。
  ※旧・次にやること②③ (個人用モードの名前・プロキシ) はバックログと「やらないこと」に吸収済み

**触ったら実機で確かめること。** 単体テスト (jsdom) は `hidden` も `disabled` も素通りするので
「画面で本当に押せるか」が分からない。実際、STEP 1 を入れたとき E2E が「見えない要素」で
止まったのも、編成の組み方が3タブにまたがっていたのも、**実際に触るまで気づけなかった**。
`scripts/e2e-board.mjs` / `e2e-import.mjs` / `e2e-picker.mjs` がその役目。

新しいPCで始めるときは **README.md → AGENTS.md → このファイル** の順に読むこと。

---

## 🎯 方針: このサイトは「ユニオンレイドが主役」

上流 nikke-calc は**ソロレイド向けの汎用計算機** (手で編成を組み、条件を詰め、タイムラインを読む) として作られている。
しりすこスクワッドはそれを**ユニオンレイド (3凸・同じニケは1度だけ) を決める道具**に作り替えたものであり、
以下を設計の原則とする (2026-09-01 ユーザー決定)。

- **入口は 3凸ボード**。最初に見せるのは「自分の育成でどの凸に何を持っていくか」であって、計算機の顔ではない
- **ソロレイド由来の要素は主役にしない**: 上流のヒーロー (「NIKKE スカッド計算機」の大見出し)・韓国コミュニティへの導線・
  enikk (ソロ順位取込)・更新履歴の自動表示は消すか控えめにする。計算機タブは「詳細計算」として残す (できることは減らさない)
- **新しい画面のトーンは 3凸ボードのモック** (https://claude.ai/code/artifact/eed3879e-a1c9-4c44-bae4-c09eebb67a84):
  軽いブランド行「しりすこスクワッド BETA」・1000px 中央寄せ・白カード・ClaudeDesign のトークン
- 迷ったら「ユニオンレイドの3凸を決める人が、この画面で何を知りたいか」で判断する

---

## ✅ 3凸ボード (2026-09-01 完成・公開済み) — 作りの記録

**承認済みモック**: https://claude.ai/code/artifact/eed3879e-a1c9-4c44-bae4-c09eebb67a84
→ 2026-09-01 に自宅Mac で実装し、既定ビューにした。モックとの差分と残りは下の「🔜 残っている宿題」。

### 作ったもの
- `site/src/raid-board.ts` — 純ロジック。盤面の保存 (`nikke-raid-board-v1`)・被り検出・代案 (外す/譲る)・
  空き枠の候補 (使用済みを外す)・**被りなし最大の総当たり** (`bestTriple`: 候補 ≤ 15 なので 455 通り)・
  盤面用の戦闘条件 (`boardBattle`: 癖なし + ボスのコードと防御力)。テストは `raid-board.test.ts` (21件)
- `ui.ts` の「3凸ボード」ビュー (`data-view="board"`、タブの先頭・既定ビュー)。描画と計算の呼び出しだけ。
  点数は既存の経路 (`requestForDeck` → `cache` → `client.simulate`) で、並列は計算機の並列設定に従う
- 取込の帯 (`data-board-sync-*`) は計算機タブの `data-sync-*` と同じ状態を映す (押すと同じ処理)
- 配色トークン `--el-*-rgb` (帯・棒) と `--el-*-text` (文字、4.5:1 を満たす濃い側) を `:root` に追加

### 動き方 (テストで固定してあること)
- ボスを選ぶ → `counterOf` で有利コードの案が入る (点数が分かっている案があれば最大、無ければ案1) → その枠を計算
- 被りは赤いチップ + 枠内の警告。「被りを解いて組み直す」で **外す/譲る の両方を計算し、合計が大きい側**にする
- 「被りなしで最大の3凸を探す」= 全ボス × 全案を計算 → `bestTriple`。3つ組めなければ 2つ・1つと落として理由を言う
- 「残りで一番出るボスを探す」= 他の枠で使った人を外した案を全部計算 → 最大。全員使用済みなら「入れられる案がありません」
- 「詳細計算へ」= 枠の編成を計算機のデッキに入れ、ボス条件 (`bossBattle`) を条件パネルに載せて計算機タブへ

### ページの顔 (2026-09-01・方針「ユニオンレイドが主役」に沿って)
- 上流のヒーロー (「NIKKE スカッド計算機」の大見出し・LOCAL SIM の円・説明文) と韓国掲示板 (dcinside) への帯を消し、
  モックと同じ軽いブランド行「しりすこスクワッド BETA」に (`.brand`)。対応人数・原作への謝辞は右側の小さなチップ
- タブは細い丸チップに (`.view-tab`)。盤面のときだけ shell を 1000px 中央寄せ (`.site-shell.is-board`、switchView が付ける)。
  計算機など他のタブは上流の 1440px のまま
- 盤面はパネルの囲いを外して白地に直接 (`.board-panel`)。カードの罫線・影はモックの値
- 見た目の確認は `site/scripts/shots.mjs` (Playwright-core + インストール済み Chrome)。各タブ × 390/820/1280px を撮り、
  **横にはみ出す要素があれば落ちる**。`google-chrome --headless --screenshot` は 420px 幅で正しく組まないので使わない

### 実機確認 (2026-09-01 自宅Mac・`scripts/e2e-board.mjs` = Playwright + 本物の Chrome + Pyodide)
- [x] 案の保存 → ボス選択 → 計算 → 被りの解消 → 被りなし最大 → 空き枠の探索まで**一連が通る・エラーなし**
- [x] 所要時間: 1計算 **7.4秒** (180秒戦闘)、並列3レーンで被りの代案3件も **7.5秒**、候補3件の全探索 6.3秒
      → 15候補でも 40秒程度の見込み。**90秒の粗探索は当面不要** (必要になったら順位相関 0.988 の実測を根拠に入れる)
- [x] スマホ幅の見え方 — 390/820px で縦1列に落ち、はみ出しなし (`scripts/shots.mjs` で確認)
- [x] 全員が被る枠を解いたとき: 「誰も残りません / 空になります」と言い、空になった枠はボスごと空に戻す
      (「残りで一番出るボスを探す」が使える状態にする) — E2E で見つけて直した
- [ ] **人の目での確認**は未 — 実際の育成を取り込んだ状態で数字の妥当性を見てほしい

## ✅ 個人利用への作り替え (2026-09-02)

**方針転換**: 「個人の利用だけを考えて、とにかく簡潔に」「戦闘結果とダメージ計算の部分だけ使う」
(ユーザー決定)。エンジンとの境目は `simulate(request) → result` の1本だけなので、
**計算部分に一切触れずに**画面側を落とせた。

- **削除**: ユニオン運営 / enikk / 自作ニケ / PNGレポート / 精密数値CSV / 共有サーバ関連3つ /
  外部リンク集 / 更新履歴。**コード 6,651行 + CSS 279規則**。バンドル 327kB → 199kB
- タブは **3凸ボード / 計算機 / 育成状況 / 属性別編成** の4つ
- 「マイロスター」→「育成状況」に改名 (何の画面か伝わっていなかった)
- **「優越コード」は誤り。正式には「有利コード」** (26箇所)

### 取り込み (BlaBlaLINK)
- **STEP 1 が入口**。取り込む前は盤面を出さず、「読み込み → 3凸」の順に進む
- プロキシ未デプロイでも動く道: **自分のブラウザでスニペットを実行 → 貼り付け**
  (`personal-scan.ts`)。自分のセッションなので**公開設定も Cookie の保守も不要**
- 自分の識別子は **実際に照会が通った候補を採用**する (GetUserInfoNew の値は
  ゲームの照会に使えず InvalidUid になる — 実機で判明)。
  **自分のプロフィールページ (`?openid=…`) を開いた状態で実行する**のが確実
- 貼るコードは画面に全文出す。`scripts/print-snippet.mjs` で同じ文字列を取り出せる
  (ソースを切り出すとテンプレートリテラルのエスケープが一段多く壊れる)

### 編成の組み方 (実使用の指摘から)
- **枠の中で直接ニケを選べる** (以前は3タブ5手順)。画像タイル + 有利コード優先 + 戦闘力順
- **★ お気に入り** (`favorites.ts`) — 印を付けた人が最優先。「★だけ」で絞れる
- **B1/B2/B3 の絞り込み**と、バーストの内訳・欠けている段階の表示
- **候補を比べる** — 1ボスにつき最大3件、まとめて計算して**一番出た候補に印**
- 他の凸で使っている人は**選ぶ時点で押せない** (3凸は同じニケを1度だけ)

### データ由来ラベルの日本語化 (完了)
オーバーロード・上級モードの項目名は `stat-names.ts` の対訳をキーで引く。
キューブの名前と効果文は **CDN の日本語ロケールから引いた公式表記**
(`scraper/cube_ja.py` → `data/cube-name-ja.json`)。推測していない。
**新しいキューブが増えたら build が落ちる** (キャラ名と同じ規則)。
実機で全4タブを走査し、表示上のハングルはゼロ。

## ✅ ここまでに入れたもの (すべて main に push 済・Codex レビュー済)

### 公開
- https://furu1018.github.io/shirisuko-squad/ (GitHub Pages・`main` push で自動デプロイ)
- リポジトリ: https://github.com/Furu1018/shirisuko-squad (public・MIT)

### v1 の①〜⑤
| # | 内容 | 主なファイル |
|---|---|---|
| ① | 取込のワンボタン更新 + マージ規則 | `sync-meta.ts` / `roster-merge.ts` |
| ② | 育成状況 (取り込んだ育成の一覧) | `my-roster.ts` |
| ③ | 属性別編成 (5属性 × 最大3案・比較) | `element-plans.ts` |
| ④ | ボス条件での再評価 (基準と並記・順位の入れ替わりを明示) | `element-plans.ts` / `union-bosses.ts` |
| ⑤ | ライトテーマ (PAD の ClaudeDesign) | `styles.css` / `timeline.ts` |
| ⑥ | 3凸ボード (入口・被りを含めて3枠まとめて決める) | `raid-board.ts` / `ui.ts` の board ブロック |

### 日本語化
UI 全ファイル。**内部キー (韓国語のキャラ名・属性・保存値・共有コード・CSVヘッダ) は翻訳しない**。
表示だけ `display-name.ts` の `labelFor()` / `elementLabel()` / `growthLabel()` などを通す。
対訳の正本は `data/name-map-ja.json` (200件)。新キャラの日本語名が無いと **build が落ちる** (仕様)。

---

## ⚠ 触るときに気をつけること

- **エンジン (`calculator/*.py`) とデータ (`data/parsed_*.json`) は無改変**。
  精度と上流同期の生命線。CI の `context.snapshot` (ゴールデン29件) が守っている
- **localStorage のキーは `nikke-` 接頭辞を必ず維持**。
  GitHub Pages では本家しりすこPAD (`/shirisu-pad/`) と**同一オリジン**なので localStorage を共有する。
  現在の衝突はゼロ (PAD=`shirisuPad.*`/`shirisuko_*`、こちら=`nikke-*`)
- **`data-*` 属性の衝突に注意**。`data-roster-empty` を足したらキャラ検索の既存要素と衝突し、
  検索側の処理が育成状況のパネルを操作する事故があった (→ `data-myroster-*` に改名)
- **取込のマージ規則を壊さない** (`roster-merge.ts`)。
  育成6項目は取込値で上書き / 操作4項目 (速射・バースト運用など) は維持。
  オーバーロード・装備・スキルは**来たキーだけ重ねる** (列が一部だけの CSV で残りを消さないため)。
  分類漏れは型エラーになるよう固定してある
- 有利コードの対応表 `BEATS` の**正本は `calculator/damage.py` の `_CODE_ADVANTAGE`**。
  テストがエンジンのソースを読んで一致を確認する

---

## 🧭 構造と使い勝手の評価 (2026-09-02・Codex と合同)

コードベース全体と導線を Codex と両面から評価した。**構造の判定**: 純ロジックの分離 (raid-board 等) は良い土台だが、
`ui.ts` (4,900行・27節・data属性144種・メッセージ表示95箇所) は「小さな追加なら耐えるが、改善を安全に積むには限界域」。
**使い勝手の判定**: 盤面主線は方針に合っているが、初回取込の摩擦と、似た機能の呼び分け不足が主な弱点。

### 改善バックログ (価値÷労力の順・S/M=労力)

1. ✅ **[UX/M] 取込データの端末間移送** — 「この端末の育成データを書き出す」を STEP 1 と育成状況に追加
   (貼る側 NKP1- は動作済み)。コンソール手順をスマホで繰り返させない。※「次にやること①」と同一
2. ✅ **[UX/S] 2つの最適化ボタンの命名分け** — 「選んだ3属性で最適化」/「全ボス・保存候補から自動探索」のように
   目的で分け、直下に探索範囲と上書き対象を一行で示す
3. ✅ **[UX/S] ステータス行の改善** — `[data-board-status]` に aria-live、押したボタンの近くにも進捗/結果、
   処理中はボタン文言を「計算中 2/3」に
4. ✅ **[UX/S] 呼称の統一** — 「案」(属性別編成タブ) と「候補」(盤面) は同じ ElementPlan。「保存候補」に統一し、
   タブ名も「保存候補・比較」へ。数値は「この条件で計算済み / 基準条件の登録値 / 未計算」を常時明示。
   タブの空状態の文言も盤面経由の導線 (「候補に加える」) に更新する
5. ✅ **[構造/M] 計算トランザクションの分離** — 「計算→登録→保存」を DOM から切り離した ScoreRunner に
   (simulate・cache・battle snapshot を注入)。盤面比較と属性別比較の重複を一本化し、jsdom なしでテスト可能に
6. ◐ **[構造/M] `mountRaidBoard(root, deps)` の切り出し** — **下ごしらえまでで止めた**。判断の根拠は下記
7. ✅ **[UX/S] ピッカーの ★ を実ボタンに** — 今は span+click で**キーボードから押せない**。aria-label と押下状態、
   使用中タイルの可視表示、パネル開閉時のフォーカス移動
8. ✅ **[構造/M] prefetch を注入可能に** — 700ms のバフ先読みがテストを汚す問題を fake timer の個別回避でなく
   scheduler 注入で解く (これまでに2件のテストで個別回避済み)
9. ✅ **[構造/M] localStorage の実装契約を一枚に** — 所有者・schemaVersion・移行・削除範囲。
   **完全初期化に漏れあり**: 削除済み機能のキー (`nikke-enikk-v1/v2` `nikke-custom-v1`) や
   `nikke-parallel-v1` `nikke-detail-damage-v1` `nikke-notice-seen` が消えず残る。cache 30件は盤面探索15件で
   押し出される前提も明記
10. ◐ **[構造/S] 小さな負債掃除** — **ui.ts は済み** (韓国語コメント238行を全部日本語に · 残り0)。
    消した機能の CSS (`.enikk-*` `.notice-card` `.report-card` `.brand-tools .notice-open`) と、
    参照先ごと消えていた skip 済みテスト2件も削除。**残り**: 他ファイルの韓国語コメント
    (`character-settings.ts` 102行 / `styles.css` 90行 / `timeline.ts` 84行 / `types.ts` 81行 /
    `share-code.ts` 79行 / `model.ts` 28行 = 464行)。share-code.ts は battle codec の利用が
    残るので、消すかどうかは用途単位で別途監査すること

### ⑥ を «下ごしらえまで» で止めた理由 (2026-09-03 に計測)

やったこと (どちらも済み・コミット済み):

- **① 取り込み UI を盤面ブロックから出した** — 盤面の中に «自分の育成をこの端末に入れる» 側の
  176行が同居していて、盤面だけを切り出せない状態だった。位置を移しただけで中身は変えていない
- **② 画面の小道具を `dom.ts` へ** — `el` / `createText` / `element` / `createElementIcon`。
  どの画面にも属さないので、ui.ts に置いたままだと切り出すたびに連れて行くことになる

計測 (`tsc` に «仮に切り出した» ファイルを食わせて未定義名を数える):

| | 下ごしらえ前 | ①② の後 |
|---|---|---|
| 盤面ブロック | 1,178行 | 1,002行 |
| 注入か移動が要る名前 | 46種 | 28種 → **実質17種** |
| うち書き換わる状態 (let) | 15種 | 10種 |

28種のうち `renderBoard` / `renderPlans` / `readBoard` / `writeBoardFrom` は前方宣言された `let` で、
中身を入れているのは盤面ブロック自身 — 切り出せば**戻り値**になるので注入ではない。

**それでも ③ に進まなかった判断**: 残る1,002行はほぼ DOM の組み立てで、**判断の中身は既に外に出ている**
(`raid-board.ts` の `bestTriple` / `openSlotCandidates` / `clashOptionsFor` にテスト26件、
`score-runner.ts` に9件)。17個を注入する口を開けても、テストできるようになるものが増えない。
広い継ぎ目を作る risk のほうが大きいと判断して止めた。**再開するなら** ①② の位置から、
17種を1つの «盤面が触ってよいもの» オブジェクトにまとめる形が素直。

### やらないこと (評価で確認した罠)

- レート制限なしの BlaBlaLINK プロキシを摩擦解消のために急いで公開しない (Cookie と BAN リスク・締切なし)
- 「属性別編成」タブを冗長に見えても消さない — 盤面の候補ライブラリ + ボス条件の再評価を担う。直すのは名称と導線
- スナップショットの重複を DRY 目的で参照化しない — 保存時点を保つ意図的なコピー
- 90秒粗探索・大規模ソルバーを先回りで足さない / 共有・ユニオン運営・ランキングを復活させない

## 🧭 使い方に合わせた作り直し (2026-09-03)

利用者の実際の流れは **«各属性の編成候補を貯める → 今回のボスを登録する → まとめて計算する →
それぞれの結果を見る → 最適な3凸を探す»**。**有利属性でしか凸らない**ので、候補が当たるボスは
1体に決まる (候補 × ボスの総当たりは要らない)。

分かったこと: **⑤の «探す» は既にあった** — 「全ボスから自動で探す」(`bestTriple`) と
「この3属性で最適化」(`bestForElements`、同じ属性を2回選べる) の両方。足りなかったのは
**順番と見せ方**で、«探す» を押した瞬間に裏で計算していたため、何十秒も «押しても無反応» だった。

| | やりたいこと | 対応 |
|---|---|---|
| ① | 各属性の編成候補を登録 | ✅ 上限を 1属性3件 → **10件** (5属性で最大50) |
| ② | 今回のボスを登録 (名称・コアの有無等) | ✅ `boss-setup.ts`。名前・防御力・コア(px)・パーツ。属性は編集不可 |
| ③ | まとめて計算 (進捗バー) | ✅ 「ぜんぶ計算」+ 進捗バー。**盤面の探索と同じ条件**で計算する |
| ④ | それぞれの結果表示 | ✅ 候補ごとに理論値 + **どの条件で出した値か** (「今回のボス条件」/「条件が変わりました」) |
| ⑤a | 属性関係なく最適3凸 | 既存 (「全ボスから自動で探す」) |
| ⑤b | 3属性を選んで最適 | 既存 (「この3属性で最適化」)。3属性バラバラも、1属性2回も可 |

**③ が効いている証拠** (実機 `scripts/e2e-batch.mjs`): 「ぜんぶ計算」の後に
「全ボスから自動で探す」を押すと **0.2秒**で答えが出る (1計算7秒台なので、計算し直していない)。
条件がずれると全部計算し直しになるので、この台本が見張っている。

## 🧩 レイド準備ボード (2026-09-03) — Codex と議論して作り直した画面

**問題**: ボスの登録と候補一覧が別々の節にあり、**順序も «属性» の意味も食い違っていた**。
ボスの並びは 電撃→灼熱→風圧→水冷→鉄甲、候補の並びは 灼熱→水冷→風圧→電撃→鉄甲。
しかも同じ «電撃» でもボスの属性か編成の属性かで意味が逆。「レイタンスに使うのは鉄甲編成」を
毎回頭で変換してから、別の順に並んだ5番目を探すことになっていた。

**直したこと**: **1行 = 1ボス**にまとめた (`renderPlans`)。左が今回のボス (名前・防御・コア・パーツ)、
右がそのボスに有利な編成の候補。対応表は `counterOf` が正本で、テストで5組すべてを固定してある
(逆にすると全候補が間違ったボスに当たる)。

| ボス | 有利な編成 |
|---|---|
| 電撃 (レイタンス) | 鉄甲 |
| 灼熱 (トゥームストーン) | 水冷 |
| 風圧 (モダニア) | 灼熱 |
| 水冷 (リビルドビッグトルソー) | 電撃 |
| 鉄甲 (アニヒリオ) | 風圧 |

- **顔ぶれは立ち絵**で並べる (計算機のニケ一覧・枠のピッカーと同じ作り)。文字だけだと
  5人の編成が «ぱっと見» で読めない。名前も下に残す — 立ち絵だけだと似た絵のニケや
  持っていないニケを見分けられない。狭い画面では**5列のグリッド**にして、幅に関係なく
  5人が1行に並ぶ (固定幅にすると端末ごとに折り返す境目を探すことになる)
- **行の状態** (最新 / 条件が変わりました / 未計算 / 候補なし) — どの行に手を入れれば前に進むかが読める
- **段の集計** (候補 / 計算済み / 条件が変わった / 未計算) を「ぜんぶ計算」の隣に
- **「この行だけ計算」** — 1行足しただけで全部を回し直さない
- **「最適3凸を探す」** ボタン — 以前は文章で «盤面の釦を使います» と言うだけで行き先が無かった
- **取り込みの状態**をレイド準備の先頭にも (最終取込に**絶対日時**を追加。相対だけだと
  前のシーズンの取込と区別できない — レイドは2週間に1度)

**タブを4つ→3つ**: レイド準備 / 最適3凸 / 編成を作る・詳細計算。既定は取り込み済みなら
レイド準備、まだなら最適3凸タブの STEP 1 (取り込みを促す画面)。育成状況は主タブから外し、
取り込みの帯から開く。

**編成モーダル (2026-09-03 追加)**: 行の「編成を組む」/ 候補の「直す」で開く。
ニケ選び (盤面と同じピッカーを共用) → 育成とハーモニーキューブ (個別設定) → 保存まで
この中で終える。以前は «詳細計算タブで組む → レイド準備に戻る → 足す» で、どこで編成を
作るのかが画面から読めなかった。**取り込んだ育成値を土台に敷く** (敷かないと個別設定を
入れた瞬間に既定値 (最大) から始まり、取り込んだ値が消える)。候補を直したら**前の理論値は
必ず捨てる** (`updatePlan`) — 顔ぶれが変われば前の数字はもうその編成の値ではない。

**文言の整理 (同日)**: «計算» が計算機タブ・理論値を出す・3凸を探すの3つを指していた。
理論値を出す操作は「理論値をぜんぶ出す」「この行の理論値を出す」、タブは「編成を作る・詳細計算」、
候補の状態は「理論値あり / 理論値まだ / 条件が変わった」に統一。

**追加とコピーの整理 (同日・Codex と検討)**: 行の主釦は「＋ 編成を追加」の1つだけ。
«詳細計算の編成を足す» は行から消し、モーダル内の「詳細計算のデッキを読み込む」に吸収
(入口が二筋だと、どこから登録するのかが読めない)。候補に「コピー」— 写しを**新規として**
モーダルで開き、どこか変えて保存すると別候補になる (何も変えないと重複で弾かれるのが
«どこか変えて» の合図)。10件の行ではコピーを押せない。育成・キューブは「詰める (任意)」を
押した人にだけ出す — «選んで保存» が最短。**「理論値をぜんぶ出す」は出してある候補を
飛ばす** — キューブを1件変えて押し直しても、その1件ぶんの7秒で済む。
残した論点: 保存した候補は**保存時点の育成値で固定**される (ロスターを取り込み直しても
追従しない)。再現性のための仕様だが、文言で明示している (「そのまま保存すると、いまの
育成値でこの候補が固定されます」)。

**キューブをまとめて付ける (同日)**: 実運用は «ふだん全員リロ速/弾チャ → ボスによって
数人をパーツ/貫通/分配に付け替える»。編成モーダルに「キューブをまとめて付ける」
(キューブ + Lv → 全員に付ける) を置いた。畳んだままでも使える位置。選択肢には効果を
添える (「レリックベアーキューブ — リロード速度」) — 公式名だけではどれがリロ速か
分からない。設定が無いニケは取り込んだ育成値 (無ければ既定) から起こしてキューブだけ
差し替える。

**バッファーのテンプレートとキューブの見える化 (2026-09-04 職場PC・ユーザー要望)**:
- **型** (`squad-templates.ts`・nikke-templates-v1・上限6): B1/B2 の定番を型として保存し、
  編成は «型から始めてアタッカーを足すだけ» に。型は**属性を持たない** (同じサポーターを
  全属性で使う)。当てるときは**空き枠にだけ**入れ、既に居るニケは二重に入れない。
  キューブ等の個別設定も型と一緒に運ぶが、触ってある設定は型で潰さない
- **キューブ略称バッジ**: 候補の顔タイルとモーダルの顔タブに リロ速/弾チャ/貫通/パーツ/分配
  などの略称を出す (効果の言葉→呼び名の表・新キューブは効果の先頭4文字)。正式名と Lv は
  title で読める。候補のスナップショットが持つキューブだけを出す — ロスター任せのニケに
  断定して出すと嘘になりうる
- キューブ一括の選択肢にも効果を併記済み (「レリックベアーキューブ — リロード速度」)
- **«いまの5人» も顔タイルに** (同日): ピッカー上部の編成表示が文字チップのままだった
  (棚も一覧も絵なのに肝心のここだけ文字 — 利用者の指摘)。B帯・✕・名前・◀▶ 付きの
  タイルへ。ついでに .board-picker-slot の二重定義 (グリッドのタイルと行で衝突) を解消

**格下げ**: 「ボス条件で確かめる」と「基準条件で比べる」は «詳しく確かめる» に畳んだ。
どちらも «ぜんぶ計算» とは条件が違う (回避区間の扱い・鮮度が付かない) ので消さず、
順位が入れ替わる理由を調べたいときだけ開く。

**BlaBlaLINK について**: `VITE_BLABLA_PROXY` が空なので **«URLを貼るだけ» の連携は公開サイトに
存在しない**。動くのはコンソールのスニペット (PC のみ)・CSV・端末間移送。促す文言はこれに合わせてある。

## 🔍 Fable + Codex の再評価 (2026-09-03) — 対応済み

**Fable (実機・使い心地)** — 前回の不満①②③④⑦はすべて実機で解決を確認。新たに5点:

1. ✅ **狭い画面で押しても無反応に見える** (最重要) — 状態表示は盤面の最上部に1つだけで、
   幅390pxで属性の行から押すと **y=-594px** (実測)。即座に弾かれる案内も、次の行動を含む
   結果も届いていなかった → 押したボタンの直後に `.board-said` として出す
2. ✅ **「未計算」から計算への導線が切れている** → 「下の『この3凸で計算する』で出ます」を添える
3. ✅ **「別の端末へ移す」の «この欄» が実物と合っていない** → 実際の貼り先 (「自分のブラウザで
   取り込む」の貼り付け欄) を名指しする
4. ✅ **取り込まずに試した後、帯が取り込みを勧めるのにボタンが無い** → 帯の取り込みボタンを常時出す
5. ✅ **ピッカーが60件で黙って切れる** → 「80名のうち 60名を出しています」と出す。実機台本にも見張りを追加

**Codex (コード監査)** — 4点:

1. ✅ **被り解消が失敗理由を «育成値» と決めつける** (中) — 直しても直らない相手に育成値を直させる
2. ✅ **failNote に総件数が無い** (低) — 「20件中 1件」の形に
3. ✅ **先読みの取り消しが予約済みタイマーにしか効かない** (低) — `disposed` の印で走行中も止める
4. 🔜 **モーダルにフォーカス管理が無い** (中) — `aria-modal`・初期フォーカス・フォーカストラップ・
   Escape・起動元への復帰が無い (`ui.ts` のバフ順序・完全初期化・CSV案内など)。
   **今回の変更で入った問題ではなく、元からの負債**。次の宿題として下に置く

**モーダルの焦点管理 (2026-09-04 職場PC・Codex 指摘の残り宿題を解消)**: hidden 属性の変化を
MutationObserver で観察して1箇所で面倒を見る — 各モーダルの開閉コードを1つも書き換えず、
新しいモーダルにも勝手に効く。開いたら中へ・Escape で閉じる (8モーダル中対応済みは2つだった)・
Tab は中で回す・閉じたら開いた場所へ戻る。後片付けで observer と keydown を外す。

## 🔍 使い勝手10点への実機監査 (2026-09-04) — 22件中18件を解消

Fable が実機 (Chrome + Pyodide) で «2週間に1度の1セッション» を通しで演じ、摩擦22件を
効く順に特定。上位から対応済み:

- ✅ **①計算報告の嘘** — battleNote が「コアとパーツ無し」の固定文字列で、コアを登録できる
  ようになった後も嘘をつき続けていた (筆頭指摘)。登録から組み立てる形に
- ✅ **②行の計算が無反応に見える** — 進捗が最上部のパネルにしか出ず、スマホ実測 y=-3,100px。
  押した行のボタンとバッジに進捗、完了は行のノートにも
- ✅ **③自動探索が盤面を確認なしで上書き** — 1世代 undo (「探す前の盤面に戻す」)
- ✅ **④型どおりに使うと3凸が組めず原因を言わない** — 全候補に入っているニケを名指しし、
  «別のバッファーで組んだ候補を» と案内
- ✅ ⑤検索語の残留 ⑥行の«一番»印 ⑦欠員の一言 ⑧削除の1世代undo ⑨12日超の再訪への一言
  ⑩取込後の«レイド準備へ»導線 ⑪5ボスへのジャンプチップ ⑫キューブバッジのタップ表示
  ⑬コピーの見出し ⑮Shift+Tabの背面漏れ ⑯コア→px欄への焦点 ⑰«個別設定つき»チップ引退
  ⑱検索例の韓国語削除 ⑲タブのtablist化+育成状況でも現在地維持 ⑳Ctrl+Enterで保存
  ㉑5人未満の一言 ㉒枠のメンバーも顔タイル
- 🔜 未対応: ⑭型の名前 (自動ラベルのみ) / ⑲の矢印キー移動 / ⑳のTab順の再設計 /
  モバイル実機タッチの確認
- ✅ **«キューブが効かない» の真相解明 (2026-09-04・最優先で対応)**: エンジンをネイティブで
  直叩きして実証 — **エンジンは無罪** (없음 3,320,819 → 렐릭 베어 Lv15 3,427,327 = +3.2%)。
  真相は **cube キーを省くとエンジンがそのキャラの既定キューブ (多くは最良 Lv15) で計算する**
  こと。監査の対象は元から既定リロ速Lv15で計算されており、同じものを付けても動かなかった。
  **画面が明示された cube しか表示せず、既定が効いているのを黙っていた**のが本当の欠陥。
  対応: (1) バッジ・モーダルは**実効キューブ** (この編成 ?? 取込 ?? 既定) を出し、
  出どころを ◦ と title で示す (2) まとめて付けるに «キューブなし» を追加し、実効と同じものを
  付けたら「理論値は変わりません」と言う (3) scripts/test-bridge.py に3つの見張り —
  キューブが効くこと・無指定=既定キューブ・**サイトの既定 (settings.json) = エンジンの既定**。
  なお Blablalink 取込は未装着を明示的に 없음 で書く (blablalink.ts:254) ので健全。
  CSV は cube 情報が無く既定で計算 — 既存の注記どおり

## 🔜 残っている宿題

- [x] **実使用の指摘3件 (2026-09-03 夜)** — ①「最適3凸を探す」に**属性を決めて最適化 →** を追加
      (自分で凸属性を指定 → 最適3凸タブの属性指定行へ誘導して一瞬光らせる。自動では回さない)
      ②共有ピッカーの選択列に **◀▶ の並び替え** (並び順は結果に影響する — 同じ5人で並びだけで
      2割以上変わる実測。ボタン式なのでキーボードでも押せる) ③モーダルの「詳細計算で開く」が
      デッキを差し替えるだけで**画面を移らず「押しても機能しない」ように見えた**バグを修正
      (実際に詳細計算タブへ移る)
- [x] **取込がシンクロレベルを捨てていた漏れ (2026-09-03 発見・修正)** — スニペットは
      `outpost.synchro_level` を取得していたのに、適用側が読まず**既定 400 のまま**だった。
      シンクロは火力に直結する (エンジン実測: 同一編成で SLv400=1,960万 → SLv843=1.27億)。
      いまは取込 (プロキシ/貼り付けとも) が自動反映し「シンクロ N を反映」と言う。
      前哨基地が非公開で取れないときは今の設定を触らない (コンソールと同じ原則)。
      手入力 (`#synchro-level`、1〜1400・1161 超は推定) は従来どおり
- [x] **取り込みデータを端末間で持ち運べる** — STEP 1 の「別の端末へ移す」で `NKX1-` を
      書き出し、スマホの貼り付け欄で受ける (`transfer.ts`)。育成・お気に入り・候補・盤面・
      シンクロを運ぶ。計算結果のキャッシュと端末ごとの設定 (並列数など) は運ばない
- [ ] **人の目での数字の確認**が未 — 実際の育成を取り込んだ状態で妥当性を見てほしい。
      取り込んだ育成の**装備・キューブが入っていないニケが多い**と、計算値は実際より低く出る
- [ ] **★ お気に入りの初回登録が枠のピッカーの中でしかできない**。
      落ち着いて選べる場所 (育成状況タブなど) からまとめて付けられた方がよいかもしれない
- [ ] **「個人用」モードの名前の整理** — ユニオンレイドタブは削除したが、
      「運営が配った盤面コードを自分のスペックで回す」用途は将来また要るかもしれない
      (今は無い。必要になったら git 履歴から戻す)
- [ ] **BlaBlaLINK プロキシのデプロイ** — これがあると URL を貼るだけで取り込める
      (本家と同じ形)。`worker/README.md` の手順で Cookie を取り、
      `wrangler secret put BLABLA_COOKIE` → `wrangler deploy` → `site/.env.production` の
      `VITE_BLABLA_PROXY` を書き換える。**Cookie は期限切れで手動更新 = 運用負債**。
      スニペット方式が動く今、急ぐ理由は薄い
- [ ] **Worker にレート制限** (デプロイするなら)。下の安全性の節を参照
- [ ] **敵防御力が暫定値** (上流既定 31,784)。実測後に `site/src/union-bosses.ts` を更新
- [ ] **3凸ボードの粗探索** — 実測 (1計算 7.4秒・並列3) では当面不要。候補が増えて 1分を
      超えるようなら、90秒戦闘で絞ってから 180秒で本計算する段階を入れる
- [ ] バフ名の日本語化 (`export-settings.py` 由来)。キューブ・オーバーロード・上級モードは
      2026-09-02 に完了済み
- [ ] 「ドレイク：グレートヴィラン」の訳が未確認 (公式日本語名を見ていない)

### 見送った案 (理由つき)

- **しりすこPAD GB の編成ランキングを使う** — 実装しかけて取りやめた (2026-09-02)。
  キャラの対応付けは**日本語表示名で 199/201 一致**し技術的には可能だったが、
  ユーザーの判断で「人気順ではなく、自分でよく使う主要キャラに印を付ける」方式にした。
  人気は他人の話で、手持ちの育成とも噛み合わない。
  なお GB のシーズンは 2026-08 で**ボスが第44回と違う**という食い違いもあった

### 🔐 BlaBlaLINK 連携の安全性 (2026-09-01 調査 → 2026-09-02 に構成が変わった)

**いまの主経路はコンソール貼り付け** (`personal-scan.ts`)。プロキシは未デプロイ。

- **コンソール貼り付け (いま使っている道)**
  - ログイン状態の blablalink.com で JS を貼らせる = **自己XSS そのものの手順**。
    スニペット自体は安全 (通信先は `api.blablalink.com` のみ・外部送信なし・
    出力はクリップボードと画面の箱だけ) だが、**やり方が危ない**
  - 「ゲーム系サイトにコンソールでコードを貼る」習慣づけは、詐欺の常套手口への耐性を下げる。
    だから**貼るコードは画面に全文出し**、「自分で読んでから貼る」「他所で配られた似たコードは
    絶対に貼らない」と添えてある。CSV の道も残してある
  - 使うのは**自分のセッション**なので、公開設定も Cookie の保守も要らない。
    運営者アカウントが凍結される心配も無い — 個人利用に限ればこの方式の方が負債が少ない
  - ※ セッション乗っ取りから NIKKE 本体のアカウント奪取まで行けるかは
    BlaBlaLINK の再認証仕様次第で**未確認**。断定しないこと

- **プロキシ経由 (未デプロイ・将来の選択肢)**
  - ブラウザが送るのは公開プロフィールの URL だけ。問い合わせは Worker が
    **運営者のセッション**で行う。利用者が差し出す秘密が無いので乗っ取られる筋がない
  - ただし運営者側のリスクは実在する:
    - Worker は本物のセッションクッキーを持つ → **副アカウント必須** (README のとおり)
    - **レート制限が無く**、`Origin` チェックは curl から偽装できる (`worker/src/index.js:222`) →
      実質誰でも使える公開プロキシ。乗っ取りではなく **BAN・凍結**のリスク
    - `/health` はクッキーの値を返さず名前と長さだけ。ここは堅い
  - **デプロイするならレート制限を同時に入れること** (Worker 側だけで完結する)

---

## 🖥 PC ごとの注意

- **職場PC (Windows) で再開するとき**: `git pull` 後に `cd site && npm ci`。見た目の確認・実機E2E
  (`scripts/shots.mjs` / `e2e-board.mjs` / `e2e-loop.mjs` / `e2e-import.mjs` / `e2e-picker.mjs`) を
  使うなら `npm i --no-save playwright-core`。
  ブラウザは `scripts/launch-browser.mjs` が **Chrome → Edge の順で探す** (Windows は Edge で動く。
  明示するなら `BROWSER_CHANNEL=msedge`)。どのスクリプトも失敗すれば非ゼロで落ちる
- **職場PC (Windows)**: 全ファイル CRLF (autocrlf)。スクリプトで一括置換するときは
  **CRLF を LF に正規化してから照合**する。生の NUL を書くと git がバイナリ扱いして差分が読めなくなる
  (`element-plans.ts` で一度やった)
- **自宅PC (Mac) は Desktop が iCloud 同期**: リポジトリが `~/Desktop` にあるため、生成物
  (`site/public/runtime` `site/public/characters` など数百ファイル) が iCloud に退避 (dataless 化)
  されたり **「calculator 3」「characters 2」のような競合コピー**が湧いたりする。
  2026-09-02 に実害が出た: sync-runtime が実体化待ちでハング → build (rolldown) がクラッシュ →
  `rm -rf` も ENOTEMPTY。対処 = 競合コピーごと生成物ディレクトリを消して作り直す
  (`rm -rf site/public/runtime "site/public/characters "*` → `npm run sync-runtime`)。
  根本対応として **2026-09-02 にリポジトリを `~/dev/しりすこスクワッド` へ移動済み** (iCloud 非同期)。
  `~/Desktop/しりすこスクワッド` は本体へのシンボリックリンクなので、Finder や既存の手順はそのまま使える。
  ※ 本家しりすこPAD (`~/Desktop/しりすこPAD`) は Desktop のままで同じ危険を持つ — あちらの会話で判断する
- **自宅PC (Mac)**: フォルダ名に日本語が入る場合、テストのパス解決に注意
  (本家PADで `fileURLToPath` に直した前例あり)
  jsdom の UI テストは負荷時に 5 秒を超える。**`vite.config.ts` に `testTimeout: 30_000` を置いた**ので
  フラグは要らない (遅さは計測の問題であって製品の問題ではない)
  それでも上流由来の `runs non-empty decks sequentially…` は稀に落ちる — 計算機の**バフ対象の先読み**
  (`prefetchBuffTargets`、700ms の setTimeout) がテストの途中で発火して `simulate` が 1 回増えるため。
  盤面のテストはボスコード付きのリクエストだけを数えて (`boardCalls`) この影響を受けないようにしてある
- **Codex 併用フラグは PC ごと**。このリポジトリでは明示レビュー方式
  (実装 → commit → codex:codex-rescue へ依頼 → 指摘対応 → push)。
  **監査を通さない push は禁止**

## 📱 スマホで見るには

育成データは **localStorage (その端末のそのブラウザだけ)** に入る。PC で取り込んでも
スマホには来ない。いまの運び方:

1. PC でスニペットを実行して出た `NKP1-…` の文字列をメモ等でスマホへ送る
2. スマホでサイトを開き、STEP 1 の貼り付け欄に貼って「取り込む」

スマホは F12 が使えないのでスニペットは実行できないが、**貼り付けだけなら動く**
(390px・タッチで確認済み)。「この端末のデータをコードで書き出す」を足せば
この往復が楽になる — 宿題の①。

## 🚀 始め方

```bash
git clone https://github.com/Furu1018/shirisuko-squad.git   # 初回
cd shirisuko-squad/site && npm ci

npm run dev        # 開発サーバ (/shirisuko-squad/ を開く)
npm test -- --run  # vitest (557件)
npm run build      # tsc --noEmit + vite build (prebuild で Python が要る)
```

配色を変えたら `npm run check-contrast` — styles.css を読んで文字色と背景の組み合わせを検算し、
小文字の基準 (4.5:1) を下回るものがあれば**落とす**。**build の前と CI で自動的に走る**ので、
基準割れのまま気づかず出すことはない (現在 400規則 / 754通りの組み合わせ。背景を指定した規則は
下地3面それぞれで試すので、規則の数と組み合わせの数はずれる)。

**`opacity` で文字を薄めるのは禁止**にしてある。要素を薄めると、その中の文字がどこまで下地に
溶けるかは DOM の入れ子が分からないと決まらず、CSS だけを読むこの検算では原理的に追えない
(実際、親 `.is-off` の下の補助文字が 2.9:1 まで落ちているのを見逃していた)。
「薄さ」は色で表す (`color: var(--ink-2)`)。逃げ道は2つだけ:

- 押せない部品 (`:disabled`) — WCAG の対象外
- 読ませる文字ではない飾り — 直前の行に `/* contrast-ignore: 理由 */`

`node scripts/verify-contrast-gate.mjs` で**検算そのものが効くか**を確かめられる。
2種類の壊し方でわざと壊し、落ちることを見る (styles.css は必ず元に戻る)。
**この検算にはこれまで5度穴が見つかっている** — 「通っている」を「守れている」と読み替えないこと。

#### 検算が守れていない範囲 (未対応・目視のみ)

**キャラ画像の上に載る文字は原理的に検算できない**。下地が画像なので CSS からは実効の
明るさが決まらない。opacity を禁じてもこの一群は消えない — 画像の上に文字を置く設計
そのものを変えないと解けないので、**意匠の判断が要る**。該当箇所:

- [ ] `.filter-panel` の見出し (`--muted` 9.5px) — 半透明 .72 + `backdrop-filter` で、
      裏はロスターのキャラ画像。暗い絵の上では 3.2:1 前後まで落ちうる
- [ ] `.roster-burst` — キャラ立ち絵の上の 9px バッジ
- [ ] `.growth-stepper` / `.growth-core` — 星アイコンの上の 9px 白文字
      (`.growth-core` は `contrast-ignore` を付けてあるが、**文字であることに変わりはない**)

直し方の方向は2つ。①背景を不透明に寄せる (`.filter-panel` を .97 まで上げれば裏の影響は
4%以下に落ち、ぼかしの見た目はほぼ残る) ②文字に確実な下敷きを敷く。①が安く、
ゲームUIらしさを保ちたい `.growth-core` などは②が向く。

**このほか、解決できない色や背景は黙って飛ばされる**（未検査であって、合格ではない）:

- `linear-gradient()` の上の文字 — `.calculate-button` が該当
- `color-mix()` の上の文字 — `.favorite-item-note` が該当
- 自分で色を持つ子要素は、親の背景まで遡らない（前方一致の見当付けまで）

見た目を確認したいときは `npm run build && npx vite preview --port 4173` を立てて、
`npm i --no-save playwright-core && node scripts/shots.mjs <出力先>` で各タブを撮る
(インストール済みの Chrome を使うのでブラウザの配布は要らない。横のはみ出しがあれば終了コード 1)。
